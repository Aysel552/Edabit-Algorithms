// Write a function that takes an integer minutes and converts it to seconds.

function convertMinutesToSeconds(minutes) {
    return Math.floor(minutes * 60)
}

console.log(convertMinutesToSeconds(15));